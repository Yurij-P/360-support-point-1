# Технічна платформа TPS360

**Статус: технічний прототип v0.2.8.** Основні доменні модулі реалізовані й покриті тестами; частина модулів заплановано для наступних етапів.

## Модулі

| Модуль | Статус |
|---|---|
| `core/` | ✅ реалізовано — базові типи, виключення, утиліти |
| `community/` | ✅ реалізовано — паспорт, ресурси, каталог, профіль готовності |
| `geospatial/` | ✅ реалізовано — OSM-межі, критична інфраструктура, рельєф |
| `threat/` | ✅ реалізовано — онтологія загроз, класифікація, ланцюжки впливу |
| `simulation/` | ✅ реалізовано — рушій, раунди, дилатація часу, LEGO, SSE, AI Copilot, AAR |
| `api/` | ✅ реалізовано — FastAPI, 30+ ендпоінтів, SSE-транспорт |
| `frontend/` | ✅ реалізовано — TypeScript API Client, OSM Map Viewer |
| `web/` | ⚠️ частково — static HTML/CSS/JS демо; повноцінний SPA не розпочато |
| `analytics/` | ⚠️ частково — AAR Telemetry реалізовано; дашборд відсутній |
| `ai/` | ⚠️ частково — AI Crisis Copilot реалізовано; інші AI-напрями — дослідження |
| `assessment/` | 🔲 заплановано |
| `mobile/` | 🔲 заплановано |

## Тести

47 тестових файлів, 456+ тест-функцій у `tests/`. Запуск: `pytest`.  
Інструменти якості: `ruff check .`, `mypy`.
