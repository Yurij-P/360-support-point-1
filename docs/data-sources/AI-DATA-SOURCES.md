# Джерела даних для підживлення AI (AI grounding sources)

**Статус:** живий реєстр (наповнюється).
**Призначення:** перелік перевірених зовнішніх джерел, якими заземлюється AI Crisis
Copilot та наповнюється паспорт громади. Узгоджено з `ai_crisis_copilot`
(`EmpiricalCrisisIncidentFact`, `official_sources_feed`), `ADR-0008` (паспорт/межі
громади), `ADR-0014` (КАТОТТГ) і `TPS360-RES-001` (виведення ресурсів з даних).

> Правило: AI не вигадує подій — він спирається лише на перевірені джерела. Кожне
> джерело перед використанням маркується рівнем довіри та способом доступу.

## Реєстр джерел

| Джерело | Тип | Що дає | Доступ / API | Статус | Використання в TPS360 |
|---|---|---|---|---|---|
| [reukraine.shtab.net](https://reukraine.shtab.net/) — «Карта Відновлення та Руйнувань» | Геопросторові дані | Пошкоджена/зруйнована інфраструктура, прогрес відбудови за регіонами | Не підтверджено (ймовірно без відкритого API) — **уточнити в адміністраторів** | 🔎 до перевірки | Паспорт громади (інфраструктура), заземлення кризових фактів AI |
| Посібник ДСНС «Рекомендації щодо дій населення у разі виникнення НС» (НМЦ ЦЗ та БЖД Дніпропетровської обл., 2023, 48 с.) | Офіційний методичний документ (офлайн PDF) | Таксономія НС (природні / техногенні / соціальні / воєнні) та **рекомендовані дії населення** по кожному типу | Офіційний документ ДСНС (не API); текст витягується локально | ✅ отримано | Звірка/розширення таксономії криз (`HazardType`), заземлення фактів AI, брифінги ролей, наповнення сценаріїв |
| **data.gov.ua (CKAN)** — **безкоштовно, без ключа** | Відкрите CKAN API | `package_search` → `datastore_search`: реальні КП громади (назва, ЄДРПОУ) з опублікованих датасетів | `GET https://data.gov.ua/api/3/action/...` (без ключа) | ✅ адаптер `DataGovUaClient` (Фаза 2); покриття залежить від опублікованих датасетів | Endowment: реальні КП громади (`source: registry`) |
| **Clarity Project API** (`clarity-project.info/api`) | Відкрите API (JSON, ключ `key`) | `edr.info`, **`vehicles.list`** (реальний автопарк — трактори, асенізатори), `realty.list` | `GET .../{method}?key=...`; `CLARITY_API_KEY` | 🟡 адаптер готовий, але data-методи **платні** (HTTP 402 на безоплатному ключі) | Endowment: реальний парк за фінансованого ключа |
| **spending.gov.ua (Є-Data)** | Транзакції публічних коштів | Видатки КП → що купували (техніка, ПММ) | Відкритий портал/API | 🔎 альтернатива | Endowment: проксі реального парку |
| **sokil/opendata.ua** | Каталог посилань (не API) | Покажчик на джерела відкритих даних України | GitHub README | 🔎 довідник | Стартова точка пошуку джерел |

| **Overpass API** (`overpass-api.de`, UI `overpass-turbo.eu`) — **безкоштовно, без ключа** | OSM-запити (Overpass QL) | Реальні об'єкти інфраструктури в межах громади (лікарні, ДСНС, ферми, трубопроводи) за bbox/area | `GET/POST https://overpass-api.de/api/interpreter?data=...` | 🔎 до інтеграції | Наповнення `infrastructure_items` паспорту реальними OSM-даними (онтологія `OSMTagMapping` вже є) |

## Кандидати на додавання (перевірити)

- Офіційні дані ДСНС України (оперативні зведення, типи НС).
- Офіційний КАТОТТГ-довідник (`directory.org.ua`) — уже інтегровано в каталог громад.
- OpenStreetMap (межі, критична інфраструктура) — уже використовується (`osm_map_viewer`).
- Норми ресурсного забезпечення (ДСНС/МОЗ/ДБН) — для нормативної оцінки endowment (Фаза 1).

## Як додавати джерело

1. Зазначити тип даних і що конкретно з них береться.
2. Спосіб доступу: відкритий API / датасет (CSV/JSON) / скрапінг / ручне
   вивантаження / за запитом.
3. Рівень довіри (офіційне відомство, OSINT, ЗМІ) та ліцензію/умови використання.
4. Куди підключається в TPS360 (паспорт, факти AI, ресурси, сценарії).
5. Статус: 🔎 до перевірки · ✅ підключено · ⛔ відхилено.

## Примітки

- **CrisisRadar** (див. [`../references/CRISISRADAR.md`](../references/CRISISRADAR.md))
  — це не джерело даних, а зовнішня платформа-референс; наведена окремо як
  порівняння можливостей.
